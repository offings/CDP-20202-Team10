<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="400dp"
    android:layout_height="650dp"
    android:background="@drawable/round_background_popup"
    android:layout_centerInParent="true"
    android:orientation="vertical"
    tools:context=".Activity.Admin.Admin_home_popupActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="600dp">

        <android.support.design.widget.TabLayout
            android:id="@+id/tab_layout_homeset"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="0dp"
            app:tabBackground="@drawable/tab_selector_for_home_set"
            app:tabGravity="center"
            app:tabIndicatorHeight="0dp"></android.support.design.widget.TabLayout>

        <android.support.v4.view.ViewPager
            android:id="@+id/home_ViewPager"
            android:layout_width="400dp"
            android:layout_height="match_parent"
            android:layout_gravity="center" />

        <ImageButton
            android:id="@+id/home_imageButton_finish"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="top|right"
            android:layout_marginTop="10dp"
            android:layout_marginRight="10dp"
            android:background="@null"
            app:srcCompat="@drawable/ic_negative_x" />

        <ImageButton
            android:id="@+id/image_passleft"
            android:layout_width="20dp"
            android:layout_height="match_parent"
            android:layout_gravity="left"
            android:layout_marginLeft="15dp"
            android:background="@null"
            app:srcCompat="@drawable/ic_pass_left" />

        <ImageButton
            android:id="@+id/image_passright"
            android:layout_width="20dp"
            android:layout_height="match_parent"
            android:layout_gravity="right"
            android:layout_marginRight="15dp"
            android:background="@null"
            app:srcCompat="@drawable/ic_pass_right" />

    </FrameLayout>

    <Button
        android:id="@+id/tab_layout_homeset_btn_confirm"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/round_button_000000"
        android:text="확인" />

</LinearLayout>